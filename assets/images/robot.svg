<svg xmlns="http://www.w3.org/2000/svg" viewBox="48 -12 570 790" width="100%" height="100%">

  <!-- =====================================================================
       CM_Pro ROBOT MASCOT — static rig
       =====================================================================
       This file is now a pure "rig": shapes, materials, and the joint
       hierarchy only — it has NO animation of its own (no <style>/
       @keyframes). All motion (the continuous idle, and the one-shot
       greet/wave/think/success/attention gestures) is driven from
       JavaScript via the Web Animations API — see
       assets/js/robot-animator.js. That split exists because the
       gestures need to be triggerable on demand from app code
       (robot.greet(), robot.wave(), ...), which a plain <img>-embedded
       SVG with baked-in CSS keyframes can never support — script in the
       parent page cannot reach into an <img>'s SVG document at all.
       This file is fetched and injected inline into the page by
       assets/js/robot-embed.js specifically so robot-animator.js can
       reach its elements by id.

       RESPONSIVE SIZING: width/height are "100%" — the viewBox alone
       defines the aspect ratio, and the element scales to fill whatever
       container it's placed in.

       VIEWBOX: measured empirically, not guessed — a headless browser
       sampled the actual rendered bounding box of #cm-pro-robot across
       full gesture playthroughs (rest pose through the peak of the
       wave/greet, elbow bend, and hand flutter all included) and the
       viewBox is that measured extent plus a safety margin on every
       side. If you change a gesture's amplitude or a limb's length,
       re-measure (see robot-animator.js's own comments) rather than
       eyeballing a new value.

       SVG HIERARCHY (mirrors the actual mechanical structure — every
       joint's transform-origin is exactly its own parent <g>, so
       rotating a shoulder carries its whole arm, rotating an elbow
       carries the forearm+hand, etc. — nothing can separate):
         #cm-pro-robot
           #shadow
           #body (breathes/bobs as one rigid unit)
             #left-leg  > left-hip-joint, left-thigh,
                          left-knee > left-shin,
                          left-ankle > left-foot
             #right-leg > (mirrored)
             #hip
             #left-arm  > left-shoulder, left-upper-arm,
                          left-elbow > left-forearm,
                          left-wrist > left-hand (3 fingers + thumb)
             #right-arm > (mirrored — this is the one that plays the
                           greet/wave gestures; drawn immediately after
                           #left-arm, both BEFORE #torso, so both
                           shoulders tuck behind the chest panel and
                           both hands render above #hip identically —
                           see the RIGHT ARM comment below for why this
                           stays correct through the raised gesture)
             #torso
             #waist-joint
             #neck
             #head > #face (the visor)

       QUICK EDIT GUIDE:
       MATERIALS  -> <defs>. bodyGrad = white/light-gray shell plates,
                     jointGrad = dark navy-black mechanical joints,
                     blackLimbs = hands + feet, visorGrad = the
                     blue-glass visor.
       HEAD/VISOR -> search "HEAD".  TORSO -> search "TORSO".
       ARMS       -> search "LEFT ARM" / "RIGHT ARM".
       LEGS       -> search "LEFT LEG" / "RIGHT LEG".

       NO RASTER FILTERS: deliberately no <feGaussianBlur>/<feDropShadow>
       and no filter:drop-shadow()/filter:blur() anywhere — a filter
       forces the renderer to rasterize that element into an offscreen
       bitmap sized for the small on-screen icon, and iOS Safari then
       just scales that fixed bitmap (doesn't re-rasterize) on
       pinch-zoom, which reads as blurry. Every glow/halo here is a
       plain radial-gradient shape fading to transparent instead
       (visorHalo, statusGlow) — real vector fills, crisp at any zoom. -->

  <defs>

    <!-- ============ MATERIAL GRADIENTS ============ -->

    <radialGradient id="headShellGrad" cx="36%" cy="26%" r="78%">
      <stop offset="0%" stop-color="#ffffff"/>
      <stop offset="65%" stop-color="#e2e5ea"/>
      <stop offset="100%" stop-color="#b7bcc4"/>
    </radialGradient>

    <linearGradient id="bodyGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#fbfcfe"/>
      <stop offset="42%" stop-color="#e4e7ec"/>
      <stop offset="78%" stop-color="#c3c8d0"/>
      <stop offset="100%" stop-color="#98a0ab"/>
    </linearGradient>

    <!-- Dark navy-black mechanical joints -->
    <linearGradient id="jointGrad" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#525a6b"/>
      <stop offset="50%" stop-color="#2b3140"/>
      <stop offset="100%" stop-color="#12151d"/>
    </linearGradient>

    <linearGradient id="visorGrad" x1="20%" y1="0%" x2="80%" y2="100%">
      <stop offset="0%" stop-color="#a9e6f2"/>
      <stop offset="35%" stop-color="#4fb8d8"/>
      <stop offset="75%" stop-color="#1f6fa8"/>
      <stop offset="100%" stop-color="#0e3f68"/>
    </linearGradient>

    <radialGradient id="visorHalo" cx="50%" cy="35%" r="65%">
      <stop offset="0%" stop-color="#7fd8ef" stop-opacity="0.5"/>
      <stop offset="70%" stop-color="#7fd8ef" stop-opacity="0.15"/>
      <stop offset="100%" stop-color="#7fd8ef" stop-opacity="0"/>
    </radialGradient>

    <linearGradient id="blackLimbs" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#3a3d44"/>
      <stop offset="30%" stop-color="#1c1e22"/>
      <stop offset="100%" stop-color="#07080a"/>
    </linearGradient>

    <radialGradient id="statusCore" cx="50%" cy="50%" r="50%">
      <stop offset="0%" stop-color="#eafeff"/>
      <stop offset="60%" stop-color="#4fd8ec"/>
      <stop offset="100%" stop-color="#1c9cbf"/>
    </radialGradient>
    <radialGradient id="statusGlow" cx="50%" cy="50%" r="50%">
      <stop offset="0%" stop-color="#4fd8ec" stop-opacity="0.55"/>
      <stop offset="60%" stop-color="#4fd8ec" stop-opacity="0.2"/>
      <stop offset="100%" stop-color="#4fd8ec" stop-opacity="0"/>
    </radialGradient>

  </defs>

  <g id="cm-pro-robot">

    <!-- Ground contact shadow — stays put while the body breathes above
         it, so the character never looks like it's floating off the
         ground it's supposedly standing on. -->
    <g id="shadow">
      <ellipse id="shadow-ellipse" cx="240" cy="737" rx="104" ry="16" fill="#000000" opacity="0.3"/>
    </g>

    <!-- Everything below is inside #body, including the legs — the
         whole character breathes/bobs as one rigid unit. -->
    <g id="body">

      <!-- ============ LEFT LEG ============
           translate(-8,0) widens the stance slightly (matched by
           right-leg's +8) — a plain SVG attribute translate, not a
           CSS transform, so it's independent of transform-origin and
           moves every child (hip joint, thigh, knee, shin, ankle,
           foot) as one rigid unit: no stretching, no gaps, nothing
           left behind. The pelvis (#hip, drawn separately, below)
           deliberately stays put and narrower than the new stance —
           that's how a real hip socket works, legs angle outward
           from a fixed hip, not the other way around. -->
      <g id="left-leg" transform="translate(-8, 0)">
        <circle id="left-hip-joint" cx="207" cy="388" r="18" fill="url(#jointGrad)"/>
        <rect id="left-thigh" x="178" y="390" width="58" height="155" rx="26" fill="url(#bodyGrad)"/>
        <rect x="187" y="398" width="10" height="128" rx="5" fill="#ffffff" opacity="0.3"/>
        <path d="M 207 408 L 207 536" stroke="#000000" stroke-width="1" opacity="0.08"/>
        <g id="left-knee">
          <circle cx="207" cy="545" r="20" fill="url(#jointGrad)"/>
          <!-- light kneecap plate layered over the dark hinge, with a
               small inner mechanical detail so it reads as a real
               joint rather than a plain ring -->
          <rect x="192" y="533" width="30" height="24" rx="12" fill="url(#bodyGrad)"/>
          <circle cx="207" cy="545" r="6.5" fill="url(#jointGrad)"/>
          <rect x="197" y="544" width="6" height="2" rx="1" fill="#0b0c0e" opacity="0.5"/>
          <rect x="211" y="544" width="6" height="2" rx="1" fill="#0b0c0e" opacity="0.5"/>
        </g>
        <rect id="left-shin" x="182" y="557" width="50" height="116" rx="22" fill="url(#bodyGrad)"/>
        <rect x="189" y="565" width="8" height="94" rx="4" fill="#ffffff" opacity="0.3"/>
        <path d="M 207 575 L 207 655" stroke="#000000" stroke-width="1" opacity="0.08"/>
        <circle id="left-ankle" cx="207" cy="673" r="16" fill="url(#jointGrad)"/>
        <g id="left-foot">
          <rect x="190" y="671" width="34" height="10" rx="5" fill="url(#bodyGrad)"/>
          <rect x="173" y="679" width="68" height="34" rx="17" fill="url(#blackLimbs)"/>
          <rect x="186" y="685" width="42" height="5" rx="2.5" fill="#7a7f88" opacity="0.4"/>
          <rect x="168" y="709" width="78" height="17" rx="8.5" fill="#141416"/>
          <rect x="168" y="709" width="78" height="5" rx="2.5" fill="#ffffff" opacity="0.25"/>
          <ellipse cx="207" cy="687" rx="24" ry="5.5" fill="#ffffff" opacity="0.12"/>
        </g>
      </g>

      <!-- ============ RIGHT LEG (mirrors the left about x=240) ============
           translate(8,0) — see the left leg's comment above. -->
      <g id="right-leg" transform="translate(8, 0)">
        <circle id="right-hip-joint" cx="273" cy="388" r="18" fill="url(#jointGrad)"/>
        <rect id="right-thigh" x="244" y="390" width="58" height="155" rx="26" fill="url(#bodyGrad)"/>
        <rect x="283" y="398" width="10" height="128" rx="5" fill="#ffffff" opacity="0.3"/>
        <path d="M 273 408 L 273 536" stroke="#000000" stroke-width="1" opacity="0.08"/>
        <g id="right-knee">
          <circle cx="273" cy="545" r="20" fill="url(#jointGrad)"/>
          <rect x="258" y="533" width="30" height="24" rx="12" fill="url(#bodyGrad)"/>
          <circle cx="273" cy="545" r="6.5" fill="url(#jointGrad)"/>
          <rect x="263" y="544" width="6" height="2" rx="1" fill="#0b0c0e" opacity="0.5"/>
          <rect x="277" y="544" width="6" height="2" rx="1" fill="#0b0c0e" opacity="0.5"/>
        </g>
        <rect id="right-shin" x="248" y="557" width="50" height="116" rx="22" fill="url(#bodyGrad)"/>
        <rect x="283" y="565" width="8" height="94" rx="4" fill="#ffffff" opacity="0.3"/>
        <path d="M 273 575 L 273 655" stroke="#000000" stroke-width="1" opacity="0.08"/>
        <circle id="right-ankle" cx="273" cy="673" r="16" fill="url(#jointGrad)"/>
        <g id="right-foot">
          <rect x="256" y="671" width="34" height="10" rx="5" fill="url(#bodyGrad)"/>
          <rect x="239" y="679" width="68" height="34" rx="17" fill="url(#blackLimbs)"/>
          <rect x="252" y="685" width="42" height="5" rx="2.5" fill="#7a7f88" opacity="0.4"/>
          <rect x="234" y="709" width="78" height="17" rx="8.5" fill="#141416"/>
          <rect x="234" y="709" width="78" height="5" rx="2.5" fill="#ffffff" opacity="0.25"/>
          <ellipse cx="273" cy="687" rx="24" ry="5.5" fill="#ffffff" opacity="0.12"/>
        </g>
      </g>

      <!-- ============ HIP ============
           Integrated pelvis: a light "belt trim" blends its top edge
           into the waist joint above. Drawn after both legs so it
           cleanly bridges down to both thigh tops. -->
      <g id="hip">
        <rect x="188" y="346" width="104" height="8" rx="4" fill="url(#bodyGrad)"/>
        <rect x="176" y="352" width="128" height="36" rx="16" fill="url(#jointGrad)"/>
        <circle cx="190" cy="370" r="8" fill="url(#bodyGrad)"/>
        <circle cx="190" cy="370" r="3.5" fill="url(#jointGrad)"/>
        <circle cx="290" cy="370" r="8" fill="url(#bodyGrad)"/>
        <circle cx="290" cy="370" r="3.5" fill="url(#jointGrad)"/>
        <circle cx="240" cy="360" r="2.5" fill="#0b0c0e" opacity="0.5"/>
        <circle cx="240" cy="380" r="2.5" fill="#0b0c0e" opacity="0.5"/>
      </g>

      <!-- ============ LEFT ARM (rests at the side, idles independently
           of the right) ============
           #left-shoulder pivots the whole arm; #left-elbow (nested)
           pivots the forearm+hand; #left-wrist (nested further) pivots
           just the hand. -->
      <g id="left-arm">
        <!-- layered shoulder housing: a lighter ring behind a dark
             joint center, instead of one plain dark ball -->
        <circle id="left-shoulder" cx="160" cy="200" r="26" fill="url(#bodyGrad)"/>
        <ellipse cx="164" cy="212" rx="20" ry="10" fill="#000000" opacity="0.06"/>
        <circle cx="160" cy="200" r="16" fill="url(#jointGrad)"/>
        <rect id="left-upper-arm" x="139" y="200" width="42" height="130" rx="19" fill="url(#bodyGrad)"/>
        <rect x="147" y="208" width="8" height="104" rx="4" fill="#ffffff" opacity="0.3"/>
        <g id="left-elbow">
          <circle cx="160" cy="330" r="17" fill="url(#jointGrad)"/>
          <circle cx="160" cy="330" r="9" fill="#1c1e22"/>
          <rect id="left-forearm" x="142" y="330" width="36" height="70" rx="16" fill="url(#bodyGrad)"/>
          <rect x="149" y="336" width="7" height="56" rx="3.5" fill="#ffffff" opacity="0.3"/>
          <g id="left-wrist">
            <circle cx="160" cy="400" r="14" fill="url(#jointGrad)"/>
            <g id="left-hand">
              <rect x="140" y="398" width="40" height="38" rx="12" fill="url(#blackLimbs)"/>
              <circle cx="149" cy="407" r="4.5" fill="#ffffff" opacity="0.2"/>
              <!-- 3 uniform, simplified fingers -->
              <rect x="143" y="432" width="10" height="23" rx="5" fill="url(#blackLimbs)" transform="rotate(-10 148 436)"/>
              <rect x="155" y="434" width="10" height="25" rx="5" fill="url(#blackLimbs)"/>
              <rect x="167" y="432" width="10" height="23" rx="5" fill="url(#blackLimbs)" transform="rotate(10 172 436)"/>
              <!-- thumb, angled outward, attached inside the palm's
                   footprint so it stays visually connected at any
                   rotation -->
              <rect x="132" y="410" width="10" height="20" rx="5" fill="url(#blackLimbs)" transform="rotate(-36 137 411)"/>
            </g>
          </g>
        </g>
      </g>

      <!-- ============ RIGHT ARM (rests at the side; plays the
           greet/wave gestures) ============
           Mirrors the left arm's geometry about x=240 at rest, AND is
           drawn in the same relative DOM position as #left-arm (right
           after it, before #torso) so it gets identical depth
           treatment: the shoulder socket tucks behind the chest panel's
           edge exactly like the left one does, and the hand still
           renders above #hip (drawn earlier) so it reads in front of
           the pelvis at the hip-rest pose. This is safe for the
           welcome/wave gesture specifically because that gesture only
           ever swings the shoulder further OUTWARD (RIGHT_HIP_BASE's
           -32deg through the anticipation dip to RIGHT_WELCOME_BASE's
           -78deg in robot-animator.js — monotonically away from the
           torso, never back across it), so the raised arm never needs
           to render in front of the torso/head — verify this holds
           with the same bounding-box sweep if you ever add a gesture
           that swings the shoulder toward center instead.
           #right-shoulder rotates the whole arm, #right-elbow (nested)
           bends it, #right-wrist (nested further) turns the hand, all
           nested so a shoulder rotation carries the rest along with it. -->
      <g id="right-arm">
        <circle id="right-shoulder" cx="320" cy="200" r="26" fill="url(#bodyGrad)"/>
        <ellipse cx="316" cy="212" rx="20" ry="10" fill="#000000" opacity="0.06"/>
        <circle cx="320" cy="200" r="16" fill="url(#jointGrad)"/>
        <rect id="right-upper-arm" x="299" y="200" width="42" height="130" rx="19" fill="url(#bodyGrad)"/>
        <rect x="325" y="208" width="8" height="104" rx="4" fill="#ffffff" opacity="0.3"/>
        <g id="right-elbow">
          <circle cx="320" cy="330" r="17" fill="url(#jointGrad)"/>
          <circle cx="320" cy="330" r="9" fill="#1c1e22"/>
          <rect id="right-forearm" x="302" y="330" width="36" height="70" rx="16" fill="url(#bodyGrad)"/>
          <rect x="324" y="336" width="7" height="56" rx="3.5" fill="#ffffff" opacity="0.3"/>
          <g id="right-wrist">
            <circle cx="320" cy="400" r="14" fill="url(#jointGrad)"/>
            <g id="right-hand">
              <rect x="300" y="398" width="40" height="38" rx="12" fill="url(#blackLimbs)"/>
              <circle cx="331" cy="407" r="4.5" fill="#ffffff" opacity="0.2"/>
              <rect id="right-finger-1" x="327" y="432" width="10" height="23" rx="5" fill="url(#blackLimbs)" transform="rotate(10 332 436)"/>
              <rect id="right-finger-2" x="315" y="434" width="10" height="25" rx="5" fill="url(#blackLimbs)"/>
              <rect id="right-finger-3" x="303" y="432" width="10" height="23" rx="5" fill="url(#blackLimbs)" transform="rotate(-10 308 436)"/>
              <rect id="right-thumb" x="338" y="410" width="10" height="20" rx="5" fill="url(#blackLimbs)" transform="rotate(36 343 411)"/>
            </g>
          </g>
        </g>
      </g>

      <!-- ============ TORSO ============
           Two layered armor panels (a chest, a taper into a narrower
           ab plate) with a visible panel seam, plus the engraved
           wordmark. -->
      <g id="torso">
        <rect id="chest-panel" x="165" y="174" width="150" height="92" rx="32" fill="url(#bodyGrad)"/>
        <rect id="ab-panel" x="183" y="248" width="114" height="86" rx="26" fill="url(#bodyGrad)"/>
        <path d="M 190 254 Q 240 265 290 254" fill="none" stroke="#000000" stroke-width="2" opacity="0.1"/>
        <path d="M 190 258 Q 240 269 290 258" fill="none" stroke="#ffffff" stroke-width="1.5" opacity="0.25"/>
        <ellipse cx="203" cy="195" rx="30" ry="15" fill="#ffffff" opacity="0.28"/>
        <!-- subtle center shading for a rounder, less flat chest -->
        <ellipse cx="240" cy="222" rx="46" ry="22" fill="#000000" opacity="0.04"/>
        <path d="M 190 316 Q 240 331 288 316 L 286 328 Q 240 342 192 328 Z" fill="#000000" opacity="0.1"/>
        <path d="M 240 184 L 240 316" stroke="#ffffff" stroke-width="1.5" opacity="0.1"/>
        <circle cx="181" cy="188" r="2.5" fill="#0b0c0e" opacity="0.35"/>
        <circle cx="299" cy="188" r="2.5" fill="#0b0c0e" opacity="0.35"/>
        <!-- engraved "CM PRO" wordmark — subtle pressed-in look, no
             bright colored patch -->
        <text x="240" y="219" font-family="'Segoe UI', Arial, sans-serif" font-size="16" font-weight="800" fill="#ffffff" fill-opacity="0.55" text-anchor="middle" letter-spacing="2.2">CM PRO</text>
        <text x="240" y="221.5" font-family="'Segoe UI', Arial, sans-serif" font-size="16" font-weight="800" fill="#5b6068" fill-opacity="0.4" text-anchor="middle" letter-spacing="2.2">CM PRO</text>
      </g>

      <!-- ============ WAIST JOINT ============
           Dark rotating collar bridging the ab plate down to the hip. -->
      <g id="waist-joint">
        <rect x="192" y="324" width="96" height="28" rx="11" fill="url(#jointGrad)"/>
        <rect x="200" y="331" width="80" height="3" rx="1.5" fill="#0b0c0e" opacity="0.6"/>
        <rect x="200" y="340" width="80" height="3" rx="1.5" fill="#0b0c0e" opacity="0.6"/>
      </g>

      <!-- ============ NECK ============
           A collar flare bridges into the head above, then the ridged
           connector, then small side plates. -->
      <g id="neck">
        <rect x="214" y="138" width="52" height="10" rx="5" fill="url(#bodyGrad)"/>
        <rect x="220" y="142" width="40" height="32" rx="8" fill="url(#jointGrad)"/>
        <rect x="228" y="150" width="24" height="2.5" rx="1.25" fill="#0b0c0e" opacity="0.6"/>
        <rect x="228" y="157" width="24" height="2.5" rx="1.25" fill="#0b0c0e" opacity="0.6"/>
        <rect x="228" y="164" width="24" height="2.5" rx="1.25" fill="#0b0c0e" opacity="0.6"/>
        <rect x="212" y="150" width="7" height="18" rx="3" fill="url(#bodyGrad)"/>
        <rect x="261" y="150" width="7" height="18" rx="3" fill="url(#bodyGrad)"/>
        <!-- "power on" status light, thin gold ring for the one
             remaining brand accent -->
        <circle cx="206" cy="159" r="8" fill="url(#statusGlow)"/>
        <circle id="neck-status" cx="206" cy="159" r="4" fill="url(#statusCore)"/>
        <circle cx="206" cy="159" r="4.8" fill="none" stroke="#D4AF37" stroke-width="1" opacity="0.8"/>
      </g>

      <!-- ============ HEAD ============
           A rounded dome plus a distinct lower jaw plate reads as
           mechanical/segmented rather than one plain ball, and sits
           directly on the neck's collar flare so it feels attached
           rather than floating above the body. -->
      <g id="head">
        <rect x="182" y="29" width="116" height="101" rx="37" fill="url(#headShellGrad)"/>
        <rect x="207" y="118" width="66" height="20" rx="10" fill="url(#headShellGrad)"/>
        <ellipse cx="208" cy="50" rx="33" ry="15" fill="#ffffff" opacity="0.4"/>
        <path d="M 210 128 Q 240 139 270 128 L 270 134 Q 240 145 210 134 Z" fill="#000000" opacity="0.12"/>
        <rect x="182" y="72" width="7" height="15" rx="3.5" fill="#0b0c0e" opacity="0.3"/>
        <rect x="291" y="72" width="7" height="15" rx="3.5" fill="#0b0c0e" opacity="0.3"/>

        <!-- ============ FACE (visor) ============ -->
        <g id="face">
          <circle id="visor-halo" cx="240" cy="76" r="57" fill="url(#visorHalo)"/>
          <rect x="190" y="44" width="100" height="71" rx="29" fill="url(#jointGrad)"/>
          <rect x="195" y="48" width="90" height="63" rx="25" fill="url(#visorGrad)"/>
          <path d="M 205 61 L 242 91" stroke="#ffffff" stroke-width="9" stroke-linecap="round" opacity="0.22"/>
          <rect x="203" y="53" width="3.5" height="8" rx="1.75" fill="#ffffff" opacity="0.4"/>
          <rect x="211" y="53" width="3.5" height="8" rx="1.75" fill="#ffffff" opacity="0.3"/>
          <rect x="216" y="93" width="48" height="10" rx="5" fill="#eef3f6" opacity="0.9"/>
        </g>

        <circle cx="240" cy="37" r="3.3" fill="#4fd8ec"/>
      </g>

    </g>
  </g>
</svg>
