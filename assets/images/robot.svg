<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 500 600" width="100%" height="100%">

  <!-- =====================================================================
       CM_Pro ROBOT MASCOT
       =====================================================================
       QUICK EDIT GUIDE — where to look when you want to change things:

       COLORS      -> see the :root custom-properties block inside <style>
                      below (garment colors) AND the gradients in <defs>
                      (metal/skin/black-limb materials). Each gradient
                      is labeled with what it's used for.
       SHIRT       -> search "SHIRT" (white top, navy collar accent)
       SHORTS      -> search "SHORTS" (white shorts, gold hem trim)
       SHOES       -> search "left-foot" / "right-foot" (sport boots)
       GLOVES      -> search "left-hand" / "right-hand"
       BAG         -> search "BAG" (hangs from the resting/left hand)
       SMILE       -> search "SMILE" (curved line under the eyes)
       ANIMATIONS  -> every @keyframes is named after what it moves,
                      e.g. wave-hand, tilt-head, foot-tap, etc.
       ===================================================================== -->

  <defs>

    <!-- ============ MATERIAL GRADIENTS ============
         These define reusable "materials" referenced via fill="url(#id)"
         all over the body. Editing a gradient here changes every part
         that uses it (e.g. changing blackLimbs changes trousers, shoes,
         AND gloves at once, since they intentionally share one material).
         If you want e.g. trousers a different black than the shoes,
         duplicate blackLimbs under a new id and swap the fill on the
         trousers <rect> elements only. -->

    <!-- Head shell material (white/grey plastic sphere look) -->
    <radialGradient id="headGlow" cx="40%" cy="30%" r="70%">
      <stop offset="0%" stop-color="#ffffff"/>
      <stop offset="70%" stop-color="#dedede"/>
      <stop offset="100%" stop-color="#b5b5b5"/>
    </radialGradient>

    <!-- "Skin"/limb material — used for bare arms, calves, torso base -->
    <linearGradient id="bodyGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#ffffff"/>
      <stop offset="40%" stop-color="#e6e6e6"/>
      <stop offset="80%" stop-color="#cccccc"/>
      <stop offset="100%" stop-color="#a3a3a3"/>
    </linearGradient>

    <!-- Face screen material (dark glass) -->
    <radialGradient id="screenGrad" cx="50%" cy="30%" r="70%">
      <stop offset="0%" stop-color="#2a2a2a"/>
      <stop offset="85%" stop-color="#0f0f0f"/>
      <stop offset="100%" stop-color="#000000"/>
    </radialGradient>

    <!-- Metal joint balls (shoulders, elbows, neck) -->
    <linearGradient id="jointGrad" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#b0b0b5"/>
      <stop offset="50%" stop-color="#787880"/>
      <stop offset="100%" stop-color="#48484a"/>
    </linearGradient>

    <!-- Eye glow material -->
    <radialGradient id="eyeGlow" cx="50%" cy="50%" r="50%">
      <stop offset="0%" stop-color="#fff5cc"/>
      <stop offset="70%" stop-color="#ffcc00"/>
      <stop offset="100%" stop-color="#cc9900"/>
    </radialGradient>

    <!-- Black material — shared by trousers, shoes, gloves, bag -->
    <linearGradient id="blackLimbs" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#444444"/>
      <stop offset="30%" stop-color="#222222"/>
      <stop offset="100%" stop-color="#050505"/>
    </linearGradient>

    <!-- CM Pro chest emblem material (brand blue, independent of navy/gold vars below) -->
    <radialGradient id="emblemGrad" cx="40%" cy="40%" r="60%">
      <stop offset="0%" stop-color="#3b82f6"/>
      <stop offset="70%" stop-color="#1d4ed8"/>
      <stop offset="100%" stop-color="#1e3a8a"/>
    </radialGradient>

    <!-- ============ FILTERS ============ -->
    <!-- Drop shadow used almost everywhere for depth/separation from the background -->
    <filter id="shadow" x="-10%" y="-10%" width="130%" height="130%">
      <feDropShadow dx="2" dy="6" stdDeviation="5" flood-opacity="0.32"/>
    </filter>
    <!-- Soft glow used on the eyes -->
    <filter id="glow" x="-20%" y="-20%" width="140%" height="140%">
      <feGaussianBlur stdDeviation="3" result="blur"/>
      <feComposite in="SourceGraphic" in2="blur" operator="over"/>
    </filter>

    <style>

      /* ============ COLOR VARIABLES ============
         Flat (non-gradient) garment colors live here so you can reskin
         the shirt/trim/bag without hunting through the markup. These
         are referenced below as fill="var(--name)". Gradient-based
         materials (blackLimbs, bodyGrad, etc. above) are NOT swappable
         via these vars — edit their stop-color values directly instead. */
      :root {
        --brand-gold: #D4AF37;   /* ACLEDA gold — trims, hems, clasps, buttons */
        --brand-navy: #003B8B;   /* ACLEDA navy — collar accent only right now */
        --shirt-color: #ffffff;  /* shirt body + short-sleeve caps */
      }

      /* ============ ANIMATION KEYFRAMES ============
         Named after the body part / effect they drive. */

      @keyframes float-body {
        /* Gentle idle float — no jump. (Legs stay inside .anim-body
           regardless; that structural change is harmless at any
           amplitude and keeps the leg/hip/shoe connections safe if
           you ever want to bring the jump back.) */
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-6px); }
      }
      @keyframes tilt-head {
        0%, 100% { transform: rotate(-12deg); }
        50% { transform: rotate(16deg); }
      }
      @keyframes wave-hand {
        /* Small sway only — the shoulder/upper-arm stays basically
           raised and still, like a real person holding their arm up
           while waving. The actual wave motion is forearm-wag below. */
        0%, 100% { transform: rotate(0deg); }
        50% { transform: rotate(-4deg); }
      }
      @keyframes forearm-wag {
        /* The real "wave" — forearm+hand rocking back and forth from
           the elbow, the way an actual hand-wave works. */
        0%, 100% { transform: rotate(0deg); }
        25% { transform: rotate(-18deg); }
        75% { transform: rotate(18deg); }
      }
      @keyframes hand-flutter {
        0%, 100% { transform: rotate(0deg); }
        25% { transform: rotate(16deg); }
        50% { transform: rotate(-10deg); }
        75% { transform: rotate(12deg); }
      }
      @keyframes left-arm-sway {
        0%, 100% { transform: rotate(0deg); }
        50% { transform: rotate(-9deg); }
      }
      @keyframes left-hand-flex {
        0%, 100% { transform: rotate(0deg) scale(1); }
        50% { transform: rotate(11deg) scale(1.06); }
      }
      @keyframes foot-tap {
        0%, 60%, 100% { transform: rotate(0deg); }
        15% { transform: rotate(10deg); }
        30% { transform: rotate(0deg); }
        45% { transform: rotate(10deg); }
      }
      @keyframes pulse-eyes {
        0%, 100% { transform: scale(1); filter: drop-shadow(0 0 2px rgba(255,204,0,0.6)); }
        50% { transform: scale(1.04); filter: drop-shadow(0 0 6px rgba(255,204,0,0.9)); }
      }
      @keyframes floor-shadow {
        0%, 100% { transform: scale(1); opacity: 0.3; }
        50% { transform: scale(0.95); opacity: 0.23; }
      }

      /* ============ ANIMATION ASSIGNMENTS ============
         transform-origin values are hand-picked to match each part's
         actual joint/pivot point — if you resize or move a body part,
         update the matching transform-origin here too, or the
         animation will pivot around the wrong spot. */

      .anim-body {
        animation: float-body 4s ease-in-out infinite;
        transform-origin: 250px 530px;
      }
      .anim-head {
        animation: tilt-head 2.2s ease-in-out infinite;
        transform-origin: 250px 185px; /* neck attachment point */
      }
      .anim-wave {
        animation: wave-hand 0.9s ease-in-out infinite;
        transform-origin: 185px 210px; /* right shoulder */
      }
      .anim-forearm-wave {
        animation: forearm-wag 0.7s ease-in-out infinite;
        transform-origin: 140px 170px; /* right elbow */
      }
      .anim-hand-flutter {
        animation: hand-flutter 0.55s ease-in-out infinite;
        transform-origin: 105px 110px; /* right wrist */
      }
      .anim-left-arm {
        animation: left-arm-sway 2.6s ease-in-out infinite;
        transform-origin: 315px 210px; /* left shoulder */
      }
      .anim-left-hand {
        animation: left-hand-flex 2.1s ease-in-out infinite;
        transform-origin: 310px 310px; /* left wrist — bag hangs from here too */
      }
      .anim-foot-left {
        animation: foot-tap 2.6s ease-in-out infinite;
        transform-origin: 213px 485px; /* left ankle */
      }
      .anim-foot-right {
        animation: foot-tap 2.6s ease-in-out infinite 1.3s; /* offset so feet don't tap in sync */
        transform-origin: 287px 485px; /* right ankle */
      }
      .anim-eye {
        animation: pulse-eyes 2s ease-in-out infinite;
        transform-origin: center;
      }
      .anim-shadow {
        animation: floor-shadow 4s ease-in-out infinite;
        transform-origin: 250px 530px;
      }
    </style>
  </defs>

  <!-- Ground contact shadow -->
  <ellipse class="anim-shadow" cx="250" cy="530" rx="90" ry="14" fill="#000000" opacity="0.3"/>

  <g id="robot-full-body">

    <!-- Everything below is inside .anim-body, including the legs — the
         whole character jumps as one rigid unit. -->
    <g class="anim-body">

      <!-- ============ LEGS & SHOES ============ -->
      <g id="legs-group">
        <g id="left-leg" filter="url(#shadow)">
          <rect x="198" y="320" width="30" height="130" rx="15" fill="url(#blackLimbs)"/>
          <rect x="193" y="390" width="40" height="120" rx="20" fill="url(#bodyGrad)"/>
          <!-- 3D touch: soft highlight running down the calf -->
          <rect x="198" y="395" width="10" height="90" rx="5" fill="#ffffff" opacity="0.35"/>
          <g id="left-foot" class="anim-foot-left">
            <!-- boot upper — taller than the old shoe, rises to meet the calf -->
            <rect x="181" y="468" width="64" height="52" rx="16" fill="url(#blackLimbs)"/>
            <!-- white sole, slightly wider than the upper for a sneaker overhang -->
            <rect x="176" y="513" width="74" height="17" rx="8" fill="#f2f2f2"/>
            <rect x="176" y="513" width="74" height="6" rx="3" fill="#ffffff" opacity="0.6"/>
            <!-- laces -->
            <path d="M 198 480 L 228 480 M 198 490 L 228 490 M 198 500 L 228 500" stroke="#ffffff" stroke-width="2.5" stroke-linecap="round" opacity="0.6"/>
            <!-- gold brand stripe along the side -->
            <path d="M 185 502 Q 213 511 241 502" fill="none" stroke="var(--brand-gold)" stroke-width="4" stroke-linecap="round"/>
            <!-- 3D touch: highlight along the top edge of the upper -->
            <ellipse cx="213" cy="474" rx="22" ry="6" fill="#ffffff" opacity="0.15"/>
          </g>
        </g>

        <g id="right-leg" filter="url(#shadow)">
          <rect x="272" y="320" width="30" height="130" rx="15" fill="url(#blackLimbs)"/>
          <rect x="267" y="390" width="40" height="120" rx="20" fill="url(#bodyGrad)"/>
          <rect x="272" y="395" width="10" height="90" rx="5" fill="#ffffff" opacity="0.35"/>
          <g id="right-foot" class="anim-foot-right">
            <rect x="255" y="468" width="64" height="52" rx="16" fill="url(#blackLimbs)"/>
            <rect x="250" y="513" width="74" height="17" rx="8" fill="#f2f2f2"/>
            <rect x="250" y="513" width="74" height="6" rx="3" fill="#ffffff" opacity="0.6"/>
            <path d="M 272 480 L 302 480 M 272 490 L 302 490 M 272 500 L 302 500" stroke="#ffffff" stroke-width="2.5" stroke-linecap="round" opacity="0.6"/>
            <path d="M 259 502 Q 287 511 315 502" fill="none" stroke="var(--brand-gold)" stroke-width="4" stroke-linecap="round"/>
            <ellipse cx="287" cy="474" rx="22" ry="6" fill="#ffffff" opacity="0.15"/>
          </g>
        </g>
      </g>
      <!-- ============ HIP ============
           Bridges the torso down to both thigh tops — keep this wide
           enough to reach x=182..318 (the thighs' outer edges) or a
           gap will reappear between the body and the legs. -->
      <g filter="url(#shadow)">
        <rect x="182" y="284" width="136" height="72" rx="22" fill="url(#bodyGrad)"/>
        <rect x="230" y="298" width="40" height="20" rx="6" fill="url(#blackLimbs)"/>
      </g>

      <!-- ============ SHORTS ============
           Two pieces (one per leg) so there's a visible leg-split gap
           at the center. White fill via the shirt-color variable
           (reused here so the shorts match the shirt if that ever
           changes). Gold hem trim uses the brand-gold variable.
           Shorter than the old trousers version — stops mid-thigh
           at y=394, well above the shoe line, leaving the calves bare.
           Width/position kept in sync with the leg stance below —
           if you change the leg x-coordinates, adjust these to match. -->
      <g id="shorts" filter="url(#shadow)">
        <rect x="182" y="300" width="63" height="106" rx="16" fill="var(--shirt-color)"/>
        <rect x="255" y="300" width="63" height="106" rx="16" fill="var(--shirt-color)"/>
        <!-- 3D touch: sheen down the front of each leg -->
        <rect x="190" y="310" width="12" height="84" rx="6" fill="#ffffff" opacity="0.5"/>
        <rect x="263" y="310" width="12" height="84" rx="6" fill="#ffffff" opacity="0.5"/>
        <rect x="182" y="392" width="63" height="8" rx="4" fill="var(--brand-gold)"/>
        <rect x="255" y="392" width="63" height="8" rx="4" fill="var(--brand-gold)"/>
      </g>

      <!-- ============ LEFT ARM (resting at hip, holds the bag) ============ -->
      <g id="left-arm-hip" class="anim-left-arm" filter="url(#shadow)">
        <circle cx="315" cy="210" r="16" fill="url(#jointGrad)"/>
        <path d="M 315 210 Q 358 238 330 290" fill="none" stroke="url(#bodyGrad)" stroke-width="20" stroke-linecap="round"/>
        <!-- short sleeve cap -->
        <ellipse cx="319" cy="228" rx="22" ry="24" fill="var(--shirt-color)" stroke="#c2c2c2" stroke-width="1.5"/>
        <path d="M 330 290 L 310 310" fill="none" stroke="url(#bodyGrad)" stroke-width="24" stroke-linecap="round" stroke-linejoin="round"/>
        <circle cx="330" cy="290" r="11" fill="url(#jointGrad)"/>
        <g id="left-hand" class="anim-left-hand">
          <circle cx="310" cy="310" r="18" fill="url(#blackLimbs)"/>
          <circle cx="310" cy="310" r="15" fill="url(#blackLimbs)"/>
          <circle cx="306" cy="305" r="4" fill="#ffffff" opacity="0.3"/>
          <!-- ============ BAG ============
               Nested inside the hand group so it moves together with
               the hand's idle flex animation instead of floating apart. -->
          <g id="bag">
            <path d="M 296 308 Q 296 282 310 282 Q 324 282 324 308" fill="none" stroke="url(#blackLimbs)" stroke-width="5" stroke-linecap="round"/>
            <rect x="285" y="305" width="50" height="52" rx="10" fill="url(#blackLimbs)" filter="url(#shadow)"/>
            <!-- 3D touch: highlight + clasp -->
            <rect x="290" y="310" width="8" height="42" rx="4" fill="#ffffff" opacity="0.12"/>
            <rect x="300" y="317" width="20" height="8" rx="3" fill="var(--brand-gold)"/>
          </g>
        </g>
      </g>

      <!-- ============ TORSO BASE ============
           Plain sphere underneath the shirt; mostly hidden once the
           shirt is drawn on top, but its highlight peeks above the collar. -->
      <g filter="url(#shadow)">
        <circle cx="250" cy="240" r="52" fill="url(#bodyGrad)"/>
        <ellipse cx="235" cy="210" rx="25" ry="15" fill="#ffffff" opacity="0.4"/>
        <!-- 3D touch: shadow crescent opposite the highlight, for a rounder sphere look -->
        <ellipse cx="262" cy="268" rx="34" ry="16" fill="#000000" opacity="0.12"/>
      </g>

      <!-- ============ SHIRT ============
           White body + navy collar accent (via the shirt-color / brand-navy
           variables up top). No gold trim on this piece by design — the
           gold accents are reserved for the trousers hem and bag clasp.
           Sits UNDER the CM Pro emblem so the logo reads as a chest patch. -->
      <g id="shirt" filter="url(#shadow)">
        <path d="M 202 196 Q 250 178 298 196 L 305 296 Q 250 315 195 296 Z" fill="var(--shirt-color)" stroke="#c2c2c2" stroke-width="1.5" opacity="1"/>
        <path d="M 236 196 L 250 216 L 264 196" fill="none" stroke="var(--brand-navy)" stroke-width="3" stroke-linecap="round" opacity="0.85"/>
        <!-- 3D touch: main fabric sheen, upper-left to lower-right -->
        <path d="M 215 205 Q 235 210 225 260" fill="none" stroke="#ffffff" stroke-width="10" stroke-linecap="round" opacity="0.3"/>
        <!-- 3D touch: secondary highlight catching the light near the shoulder -->
        <ellipse cx="216" cy="200" rx="18" ry="10" fill="#ffffff" opacity="0.35"/>
        <!-- 3D touch: soft center fold for fabric realism -->
        <path d="M 250 220 Q 245 255 250 292" fill="none" stroke="#000000" stroke-width="2" opacity="0.08"/>
        <!-- 3D touch: shadow crescent along the bottom hem, opposite the highlights -->
        <path d="M 210 285 Q 250 302 290 285 L 288 297 Q 250 313 197 297 Z" fill="#000000" opacity="0.1"/>
      </g>

      <!-- ============ CM PRO EMBLEM (chest logo patch) ============ -->
      <g id="cmpro-emblem" filter="url(#shadow)">
        <circle cx="250" cy="240" r="32" fill="url(#emblemGrad)"/>
        <circle cx="250" cy="240" r="30" fill="none" stroke="#ffffff" stroke-width="1.5" opacity="0.6"/>
        <text x="250" y="236"
              font-family="'Arial Black', 'Impact', sans-serif"
              font-size="14"
              font-weight="900"
              fill="#ffffff"
              text-anchor="middle"
              letter-spacing="0.5"
              filter="drop-shadow(1px 2px 2px rgba(0,0,0,0.5))">CM</text>
        <text x="250" y="252"
              font-family="'Arial Black', 'Impact', sans-serif"
              font-size="13"
              font-weight="900"
              fill="#ffffff"
              text-anchor="middle"
              letter-spacing="0.5"
              filter="drop-shadow(1px 2px 2px rgba(0,0,0,0.5))">Pro</text>
      </g>

      <!-- Neck piston connecting torso to head -->
      <rect x="240" y="165" width="20" height="30" rx="5" fill="url(#jointGrad)" filter="url(#shadow)"/>

      <!-- ============ HEAD ============ -->
      <g class="anim-head">
        <rect x="185" y="70" width="130" height="110" rx="30" fill="url(#headGlow)" filter="url(#shadow)"/>
        <path d="M 210 75 L 290 75 Q 305 75 305 90 L 195 90 Q 195 75 210 75 Z" fill="#ffffff" opacity="0.4"/>
        <!-- 3D touch: shadow crescent at the base of the head, opposite the top highlight -->
        <path d="M 200 165 Q 250 180 300 165 L 300 175 Q 250 188 200 175 Z" fill="#000000" opacity="0.08"/>

        <rect x="197" y="82" width="106" height="86" rx="18" fill="url(#screenGrad)"/>

        <!-- ============ EYES ============ -->
        <g class="anim-eye">
          <circle cx="227" cy="125" r="19" fill="none" stroke="#ffcc00" stroke-width="4" filter="url(#glow)"/>
          <circle cx="227" cy="125" r="14" fill="url(#eyeGlow)"/>
          <circle cx="223" cy="120" r="4" fill="#ffffff" opacity="0.8"/>

          <circle cx="273" cy="125" r="19" fill="none" stroke="#ffcc00" stroke-width="4" filter="url(#glow)"/>
          <circle cx="273" cy="125" r="14" fill="url(#eyeGlow)"/>
          <circle cx="269" cy="120" r="4" fill="#ffffff" opacity="0.8"/>
        </g>

        <!-- ============ SMILE ============
             Happy expression — bright curved line under the eyes. -->
        <path d="M 222 148 Q 250 165 278 148" fill="none" stroke="#4dd8ff" stroke-width="5" stroke-linecap="round" filter="url(#glow)"/>
      </g>

      <!-- ============ RIGHT ARM (waving) ============
           Human-like wave: the shoulder/upper-arm barely moves (small
           sway only) while the FOREARM wags back and forth from the
           elbow — that's how a real hand-wave actually works, unlike
           a full-arm pendulum swing which reads as mechanical. -->
      <g id="right-arm-wave" class="anim-wave">
        <circle cx="185" cy="210" r="16" fill="url(#jointGrad)" filter="url(#shadow)"/>
        <path d="M 185 210 L 140 170" fill="none" stroke="url(#bodyGrad)" stroke-width="22" stroke-linecap="round" filter="url(#shadow)"/>
        <!-- short sleeve cap -->
        <ellipse cx="174" cy="200" rx="23" ry="21" fill="var(--shirt-color)" stroke="#c2c2c2" stroke-width="1.5"/>
        <circle cx="140" cy="170" r="13" fill="url(#jointGrad)"/>
        <!-- forearm + hand wag together, pivoting at the elbow (140,170) -->
        <g id="right-forearm-wave" class="anim-forearm-wave">
          <path d="M 140 170 L 105 110" fill="none" stroke="url(#bodyGrad)" stroke-width="26" stroke-linecap="round" filter="url(#shadow)"/>
          <g id="right-hand" class="anim-hand-flutter">
            <circle cx="105" cy="110" r="18" fill="url(#blackLimbs)"/>
            <circle cx="105" cy="110" r="15" fill="url(#blackLimbs)"/>
            <circle cx="101" cy="105" r="5" fill="#ffffff" opacity="0.3"/>
          </g>
        </g>
      </g>

    </g>
  </g>
</svg>
